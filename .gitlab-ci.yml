stages:
    - build
    - test

before_script:
    - "dotnet restore SGame"

build:
    stage: build
    script:
        - "dotnet build SGame"

test:
    stage: test
    script:
        - "dotnet run --project SGame"
